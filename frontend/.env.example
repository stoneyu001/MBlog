# 埋点 API 端点配置示例

# 开发环境 - 使用本地后端
VITE_TRACKING_ENDPOINT=http://localhost:3000/api/tracking/batch

# 生产环境 - 使用实际域名（取消注释并修改）
# VITE_TRACKING_ENDPOINT=https://yourdomain.com/api/tracking/batch

# 或使用相对路径（如果前后端部署在同一域名下）
# VITE_TRACKING_ENDPOINT=/api/tracking/batch
