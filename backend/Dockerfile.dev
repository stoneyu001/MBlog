# 使用官方 Go 轻量镜像
FROM golang:1.20-alpine

# 设置 Go 模块代理
ENV GO111MODULE=on
ENV GOPROXY=https://goproxy.cn,direct
ENV GOSUMDB=sum.golang.google.cn

# 安装热重载工具gin
RUN go install github.com/codegangsta/gin@latest

# 设置容器内工作目录
WORKDIR /app

# 创建并准备静态文件目录
RUN mkdir -p /app/static

# 声明服务端口
EXPOSE 3000

# 使用gin启动并监听文件变化
# gin会监视所有.go文件变化并自动重启主程序
CMD ["gin", "--appPort", "3000", "--port", "3000", "--immediate", "run", "main.go"] 